projection;
strict ( 2 );
use draft;

define behavior for ZSA_EXCEL_USER_C alias XLHead
{
  use create;
  use update;
  use delete;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;

  use action UploadExcelData;

  use association _XLData { create; with draft; }
}

define behavior for ZSA_EXCEL_DATA_C alias XLData
{
  use update;
  use delete;

  use association _XLUser { with draft; }
}